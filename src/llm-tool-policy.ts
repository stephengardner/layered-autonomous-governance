// Compatibility shim. The implementation lives at
// src/substrate/policy/llm-tool-policy.ts; this re-export preserves the
// previous import path so existing consumers compile unchanged.
export * from './substrate/policy/llm-tool-policy.js';
