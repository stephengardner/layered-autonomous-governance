# Runtime

Operator-facing runtime: daemon, scheduler, file transports. Knows
nothing about what an "agent" is beyond the 8-interface host contract.

Current contents: (populated opportunistically as legacy `src/daemon/`
and `src/adapters/` modules get touched in phase 1+).
